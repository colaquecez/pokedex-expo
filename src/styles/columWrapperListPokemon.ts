import { StyleSheet } from 'react-native';

const column = StyleSheet.create({
  wrapper: {
    flex: 1,
    marginBottom: 8,
    marginHorizontal: 10,
    justifyContent: 'space-between'
  }
});

export default column.wrapper;
